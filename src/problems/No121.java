package problems;

public class No121 {
    public int maxProfit(int[] prices) {
        // 该题目要求只能进行一次买，一次卖
        // 那么只需要找出某一索引i的后面的索引中，与当前值相差最大的索引j
        int n = prices.length;
        int[] profits = new int[n];
        for (int i = 0; i < n; ) {

        }
        return 0;
    }
}
